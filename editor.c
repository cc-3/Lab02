#include <stdio.h>
#include <stdlib.h>

#define MENU ">>Nombre del archivo: "

char* expand(char *string, int currSize, int newSize){
	//IMPLEMENTE SU CODIGO AQUI
	free(string);
	return NULL;
}


int main(int argc, char* argv[]){
	if(argc == 1){
		printf(MENU);
	}
	return 0;
}